import { H1 } from '@stylin.js/elements';
import { FC } from 'react';

import { Layout } from '@/components';

const Home: FC = () => (
  <Layout>
    <H1>Home</H1>
  </Layout>
);

export default Home;
